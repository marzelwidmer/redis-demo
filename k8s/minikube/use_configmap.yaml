apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-demo
spec:
  template:
    spec:
      containers:
        - name: redis-demo
          volumeMounts:
            - mountPath: /config
              name: redis-demo-config
      volumes:
        - name: redis-demo-config
          configMap:
            name: redis-demo-config






